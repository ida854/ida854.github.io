<template>
  <div>
  <h1> {{ single.name }} {{single.family}}</h1>
  <p> Email: {{ single.email }}</p>
  <p>{{ single.kurs }}</p>

  <p>{{ single.name }} is of IT-Högskolan {{ $route.params.cat }}</p>
  </div>
</template>
<script>
import { mapState } from 'vuex'
export default {
  data () {
    return {
      single: {}
    }
  },
  computed: {
    ...mapState([
      'teachers',
      'students'
    ])
  },
  mounted () {
    const user = this[this.$route.params.cat][this.$route.params.id]
    this.single = user
  }
}
</script>
